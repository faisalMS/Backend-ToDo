spring.application.name=${APP_NAME:Full Stack To Do}
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/fullStackTodo}
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:}
server.port=800
